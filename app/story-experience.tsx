"use client";
import {PointerEvent,useRef,useState} from "react";

export type StoryPage={page:number;title:string;text:string;image_prompt:string;image_url?:string};
export type Sticker={id:string;src:string;page:number;x:number;y:number;size:number};
export type Point={x:number;y:number};
export type Stroke={id:string;page:number;color:string;width:number;points:Point[]};
export type Decoration={stickers:Sticker[];drawings:Stroke[]};
export type StoryData={id:string;child_name:string;genre:string;object_name:string;title:string;summary:string;pages:StoryPage[];stickers?:Sticker[];drawings?:Stroke[]};
const stickerSources=Array.from({length:9},(_,i)=>`/stickers/sticker-${String(i+1).padStart(2,"0")}.png`);
const colors=["#ef5f89","#ff9f43","#ffd43b","#57b77a","#4d91e8","#7558c9","#3d2940"];

function BookPage({story,page}:{story:StoryData;page:number}){const p=story.pages[page];return <><div className="visual">{p.image_url?<img src={p.image_url} alt={`${p.title} 삽화`}/>:<div className="image-wait">삽화를 불러오고 있어</div>}<span>{page+1} / {story.pages.length}</span></div><article><small>{story.child_name}의 {story.genre} 동화</small><h2>{page===0?story.title:p.title}</h2><p>{p.text}</p></article></>}

export function ReaderBook({story,onDecorate,restart}:{story:StoryData;onDecorate:()=>void;restart:()=>void}){const [page,setPage]=useState(0),[turning,setTurning]=useState<"next"|"prev"|null>(null),touch=useRef(0);function turn(n:number){if(n<0||n>=story.pages.length||n===page||turning)return;setTurning(n>page?"next":"prev");setPage(n);setTimeout(()=>setTurning(null),480)}return <section className="screen story reader"><div className={`book ${turning?`turn-${turning}`:""}`} onTouchStart={e=>touch.current=e.touches[0].clientX} onTouchEnd={e=>{const d=e.changedTouches[0].clientX-touch.current;if(Math.abs(d)>55)turn(page+(d>0?-1:1))}}><BookPage story={story} page={page}/><nav className="book-nav"><button disabled={page===0} onClick={()=>turn(page-1)}>앞 페이지</button><div>{story.pages.map((_,i)=><button key={i} className={page===i?"on":""} onClick={()=>turn(i)} aria-label={`${i+1}쪽`}/>)}</div>{page<story.pages.length-1?<button onClick={()=>turn(page+1)}>다음 페이지</button>:<button className="save-story" onClick={onDecorate}>동화 저장하기</button>}</nav></div><button className="restart" onClick={restart}>다른 이야기도 만들자!</button><style>{styles}</style></section>}

export function DecorateBook({story,finish}:{story:StoryData;finish:(d:Decoration)=>Promise<void>}){const [page,setPage]=useState(0),[mode,setMode]=useState<"pen"|"sticker">("pen"),[stickers,setStickers]=useState<Sticker[]>(story.stickers||[]),[drawings,setDrawings]=useState<Stroke[]>(story.drawings||[]),[selected,setSelected]=useState(""),[color,setColor]=useState(colors[0]),[width,setWidth]=useState(7),[ghost,setGhost]=useState<{src:string;x:number;y:number}|null>(null),[saving,setSaving]=useState(false),bookRef=useRef<HTMLDivElement>(null),drawing=useRef<Stroke|null>(null);
 function pos(e:PointerEvent){const r=bookRef.current?.getBoundingClientRect();if(!r)return null;return {x:Math.max(0,Math.min(100,(e.clientX-r.left)/r.width*100)),y:Math.max(0,Math.min(100,(e.clientY-r.top)/r.height*100)),inside:e.clientX>=r.left&&e.clientX<=r.right&&e.clientY>=r.top&&e.clientY<=r.bottom}}
 function penStart(e:PointerEvent<SVGSVGElement>){if(mode!=="pen")return;e.currentTarget.setPointerCapture(e.pointerId);const p=pos(e);if(!p)return;drawing.current={id:crypto.randomUUID(),page,color,width,points:[p]};setDrawings(v=>[...v,drawing.current!])}
 function penMove(e:PointerEvent<SVGSVGElement>){if(!drawing.current||!e.currentTarget.hasPointerCapture(e.pointerId))return;const p=pos(e);if(!p)return;drawing.current={...drawing.current,points:[...drawing.current.points,p]};const next=drawing.current;setDrawings(v=>v.map(s=>s.id===next!.id?next!:s))}
 function penEnd(){drawing.current=null}
 function startNew(e:PointerEvent<HTMLButtonElement>,src:string){e.currentTarget.setPointerCapture(e.pointerId);setGhost({src,x:e.clientX,y:e.clientY})}
 function moveNew(e:PointerEvent){if(ghost)setGhost(v=>v&&({...v,x:e.clientX,y:e.clientY}))}
 function endNew(e:PointerEvent,src:string){const p=pos(e);setGhost(null);if(p?.inside){const id=crypto.randomUUID();setStickers(v=>[...v,{id,src,page,x:p.x,y:p.y,size:112}]);setSelected(id)}}
 function moveSticker(e:PointerEvent<HTMLButtonElement>,id:string){if(!e.currentTarget.hasPointerCapture(e.pointerId))return;const p=pos(e);if(p)setStickers(v=>v.map(s=>s.id===id?{...s,x:p.x,y:p.y}:s))}
 const pageStrokes=drawings.filter(s=>s.page===page),pageStickers=stickers.filter(s=>s.page===page);
 return <section className="screen story decorator"><div className="decorate-head"><div><small>동화 저장 완료! 이제 보너스 놀이야</small><h2>내 동화책을 마음껏 꾸며 봐</h2></div><div className="mode-tabs"><button className={mode==="pen"?"on":""} onClick={()=>{setMode("pen");setSelected("")}}>✏️ 펜으로 그리기</button><button className={mode==="sticker"?"on":""} onClick={()=>setMode("sticker")}>🌟 스티커 붙이기</button></div></div>{mode==="pen"?<div className="pen-tools">{colors.map(c=><button key={c} className={color===c?"on":""} style={{background:c}} onClick={()=>setColor(c)} aria-label={`${c} 색상`}/>)}<button className={width===4?"on":""} onClick={()=>setWidth(4)}>가는 펜</button><button className={width===7?"on":""} onClick={()=>setWidth(7)}>보통 펜</button><button className={width===12?"on":""} onClick={()=>setWidth(12)}>굵은 펜</button><button onClick={()=>setDrawings(v=>v.filter(s=>s.page!==page))}>이 페이지 지우기</button></div>:<div className="sticker-tray">{stickerSources.map((src,i)=><button key={src} aria-label={`${i+1}번째 스티커`} onPointerDown={e=>startNew(e,src)} onPointerMove={moveNew} onPointerUp={e=>endNew(e,src)}><img src={src} alt="" draggable={false}/></button>)}</div>}
 <div ref={bookRef} className="book decorate-canvas" onPointerDown={()=>selected&&setSelected("")}><BookPage story={story} page={page}/><svg className={`drawing-layer ${mode==="pen"?"active":""}`} viewBox="0 0 100 100" preserveAspectRatio="none" onPointerDown={penStart} onPointerMove={penMove} onPointerUp={penEnd} onPointerCancel={penEnd}>{pageStrokes.map(s=><polyline key={s.id} points={s.points.map(p=>`${p.x},${p.y}`).join(" ")} fill="none" stroke={s.color} strokeWidth={s.width/7} strokeLinecap="round" strokeLinejoin="round" vectorEffect="non-scaling-stroke"/>)}</svg>{pageStickers.map(s=><button key={s.id} className={`placed-sticker ${selected===s.id?"selected":""}`} style={{left:`${s.x}%`,top:`${s.y}%`,width:s.size}} onPointerDown={e=>{e.stopPropagation();e.currentTarget.setPointerCapture(e.pointerId);setSelected(s.id)}} onPointerMove={e=>moveSticker(e,s.id)}><img src={s.src} alt="붙인 포동이 스티커" draggable={false}/></button>)}</div>
 <div className="decorate-footer"><button disabled={page===0} onClick={()=>{setPage(v=>v-1);setSelected("")}}>← 앞 페이지</button><strong>{page+1} / {story.pages.length}</strong>{page<story.pages.length-1?<button onClick={()=>{setPage(v=>v+1);setSelected("")}}>다음 페이지 →</button>:<button className="finish" disabled={saving} onClick={async()=>{setSaving(true);await finish({stickers,drawings});setSaving(false)}}>{saving?"저장 중…":"꾸미기 끝! 홈으로"}</button>}</div>{selected&&<div className="sticker-tools"><button onClick={()=>setStickers(v=>v.map(s=>s.id===selected?{...s,size:Math.max(70,s.size-18)}:s))}>작게</button><button onClick={()=>setStickers(v=>v.map(s=>s.id===selected?{...s,size:Math.min(220,s.size+18)}:s))}>크게</button><button onClick={()=>{setStickers(v=>v.filter(s=>s.id!==selected));setSelected("")}}>떼기</button></div>}{ghost&&<img className="sticker-ghost" src={ghost.src} style={{left:ghost.x,top:ghost.y}} alt=""/>}<style>{styles}</style></section>}

const styles=`
.reader .book,.decorate-canvas{position:relative}.book-nav{position:absolute;z-index:8;left:calc(45% + 18px);right:24px;bottom:20px;display:grid;grid-template-columns:auto 1fr auto;gap:10px;align-items:center}.book-nav>button,.decorate-footer button{border:0;background:#f9e7ed;padding:10px 15px;border-radius:99px}.book-nav>button:disabled,.decorate-footer button:disabled{opacity:.35}.book-nav>div{display:flex;justify-content:center;gap:7px}.book-nav>div button{width:9px;height:9px;padding:0;border:0;border-radius:50%;background:#e8c7d1}.book-nav>div button.on{background:#f55f91}.save-story,.finish{background:#f55f91!important;color:#fff!important}.reader .book article{padding-bottom:82px}.decorate-head{max-width:1180px;margin:0 auto 12px;display:flex;justify-content:space-between;align-items:end;gap:20px}.decorate-head small{color:#f55f91}.decorate-head h2{margin:4px 0;font-size:clamp(25px,3vw,38px)}.mode-tabs{display:flex;background:#fff;padding:5px;border-radius:16px}.mode-tabs button{border:0;background:none;padding:11px 15px;border-radius:12px}.mode-tabs button.on{background:#ffe5ed;color:#bd3d69;font-weight:700}.pen-tools,.sticker-tray{max-width:1180px;margin:0 auto 12px;display:flex;align-items:center;gap:8px;overflow-x:auto;padding:9px 12px;background:#fff;border-radius:18px;box-shadow:0 10px 28px #71374b17}.pen-tools button{flex:0 0 auto;border:1px solid #ead1da;background:#fff7fa;border-radius:99px;padding:8px 12px}.pen-tools button[style]{width:30px;height:30px;padding:0;border:3px solid #fff;box-shadow:0 0 0 1px #ddc4cc}.pen-tools button.on{box-shadow:0 0 0 3px #3d2940}.sticker-tray button{width:72px;height:72px;flex:0 0 72px;border:0;background:#fff0f4;border-radius:15px;padding:4px;touch-action:none}.sticker-tray img{width:100%;height:100%;object-fit:contain;pointer-events:none}.decorator{padding-top:20px;overflow:visible}.decorator .book{height:clamp(500px,calc(100svh - 300px),650px)}.drawing-layer{position:absolute;inset:0;z-index:10;width:100%;height:100%;pointer-events:none}.drawing-layer.active{pointer-events:auto;touch-action:none;cursor:crosshair}.placed-sticker{position:absolute;z-index:12;transform:translate(-50%,-50%);padding:0;border:0;background:none;touch-action:none;filter:drop-shadow(0 5px 5px #5e354b3d)}.placed-sticker img{display:block;width:100%;height:auto;pointer-events:none}.placed-sticker.selected{outline:3px dashed #ff5f91;outline-offset:5px;border-radius:12px}.sticker-tools{position:sticky;z-index:20;bottom:12px;margin:8px auto 0;width:max-content;display:flex;gap:6px;background:#3d2940e8;padding:7px;border-radius:99px}.sticker-tools button{border:0;background:#fff;border-radius:99px;padding:8px 13px}.sticker-ghost{position:fixed;z-index:100;width:120px;max-height:150px;object-fit:contain;transform:translate(-50%,-50%);pointer-events:none}.decorate-footer{max-width:760px;margin:14px auto 0;display:flex;justify-content:space-between;align-items:center;gap:10px}@media(max-width:850px){.book-nav{left:18px}.decorate-head{display:block;text-align:center}.mode-tabs{width:max-content;margin:10px auto}.decorator .book{height:calc(100svh - 335px);min-height:500px}.placed-sticker{max-width:25vw}}@media(max-width:560px){.book-nav{left:10px;right:10px;bottom:8px}.book-nav>button{font-size:11px;padding:8px}.reader .book article{padding-bottom:58px}.decorate-head h2{font-size:25px}.mode-tabs button{padding:9px}.decorator .book{height:520px}.decorate-footer button{font-size:12px;padding:9px}.sticker-tray button{width:62px;height:62px;flex-basis:62px}}
.mode-tabs,.pen-tools,.sticker-tray{background:linear-gradient(145deg,#ffffff,#fff6f9);border:1px solid #fff;box-shadow:inset 0 1px #fff,inset 0 -1px #edd5dd,0 14px 34px #7f42571c}.mode-tabs button.on{background:linear-gradient(145deg,#fff0f5,#ffdbe7);box-shadow:inset 0 1px #fff,0 6px 14px #c7567d22}.book-nav>button,.decorate-footer button{background:linear-gradient(145deg,#fff5f8,#f4dce4);box-shadow:inset 0 1px #fff,inset 0 -2px 4px #bd789020,0 6px 14px #76374d16}.save-story,.finish{background:linear-gradient(145deg,#ff789f,#e94b7f)!important;box-shadow:inset 0 2px 1px #ffffff70,inset 0 -4px 8px #af24543b,0 12px 25px #d9588642}.sticker-tray button{background:linear-gradient(145deg,#fffafd,#ffe9f0);box-shadow:inset 3px 4px 8px #fff,inset -4px -5px 9px #e7b4c24a,0 6px 13px #91445f15}.decorate-head h2{text-shadow:0 2px #fff,0 10px 24px #b9517422}
`;
